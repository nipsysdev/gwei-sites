{
  "tasks": {
    "dev": "deno serve --watch --env-file --allow-net --allow-env main.ts",
    "start": "deno run --allow-net --allow-env main.ts",
    "test": "deno test --allow-net --allow-env",
    "test:coverage": "deno test --allow-net --allow-env --coverage=cov/",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "check": "deno check main.ts",
    "ci": "deno fmt --check && deno lint && deno test --allow-all --coverage=cov/"
  },
  "compilerOptions": {
    "strict": true,
    "lib": ["deno.window"],
    "noImplicitAny": true
  },
  "fmt": {
    "lineWidth": 100,
    "indentWidth": 2,
    "singleQuote": false,
    "semiColons": true
  },
  "lint": {
    "include": ["src/", "main.ts"],
    "rules": {
      "tags": ["recommended"]
    }
  },
  "test": {
    "include": ["src/"]
  },
  "exclude": ["cov/"],
  "deploy": {
    "entrypoint": "main.ts"
  }
}
